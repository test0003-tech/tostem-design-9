import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TOSTEM India — Leading Aluminium Windows and Doors Manufacturer",
  description: "TOSTEM India is the world's leading manufacturer and supplier of aluminium windows and doors. Browse our premium Japanese products for durable, energy-efficient solutions.",
  keywords: ["TOSTEM", "Aluminium Windows", "Aluminium Doors", "Japanese Innovation", "Windows India", "Doors India"],
  authors: [{ name: "TOSTEM India" }],
  icons: {
    icon: "https://cdn-ildpppi.nitrocdn.com/xjROyyheOXReIMzlTkTVBhxlcelzUnWY/assets/images/optimized/rev-c76f7e6/www.tostemindia.com/wp-content/uploads/2024/07/logo-tostemindia.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
